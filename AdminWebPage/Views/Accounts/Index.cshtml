@model IEnumerable<AdminWebPage.Models.Account>

@{
    ViewData["Title"] = "Account List";
}

<h2 class="mb-4">Account List</h2>

<form method="get" class="row g-3 mb-3" id="searchForm">
    <div class="col-md-5">
        <input type="text"
               id="searchBox"
               value="@ViewBag.Search"
               class="form-control"
               placeholder="Search name..." />
    </div>

    <div class="col-md-3">
        <select id="roleFilter" class="form-select">
            <option value="">All Roles</option>
            <option value="Teacher" selected="@(ViewBag.Role == "Teacher" ? "selected" : null)">Teacher</option>
            <option value="Student" selected="@(ViewBag.Role == "Student" ? "selected" : null)">Student</option>
        </select>

    </div>

    <div class="col-md-2">
        <a asp-action="Create" class="btn btn-success w-100">Create New</a>
    </div>
</form>

<!-- Table Container -->
<div class="table-responsive shadow-sm rounded">
    <table class="table table-striped table-bordered mb-0 bg-white">
        <thead class="table-dark">
            <tr>
                <th>First Name</th>
                <th>Middle Name</th>
                <th>Last Name</th>
                <th>Email</th>
                <th>Role</th>
                <th style="width:180px">Actions</th>
            </tr>
        </thead>
        <tbody id="accountTableBody">
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.FName</td>
                    <td>@item.MName</td>
                    <td>@item.LName</td>
                    <td>@item.Email</td>
                    <td>
                        <span class="badge @(item.Role == "Teacher" ? "bg-primary" : "bg-secondary")">
                            @item.Role
                        </span>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.AccountID" class="btn btn-sm btn-warning">Edit</a>
                        <a asp-action="Delete" asp-route-id="@item.AccountID" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        const searchBox = document.getElementById('searchBox');
        const roleFilter = document.getElementById('roleFilter');
        const tableBody = document.getElementById('accountTableBody');

        function debounce(fn, delay) {
            let timeout;
            return function () {
                clearTimeout(timeout);
                timeout = setTimeout(() => fn.apply(this, arguments), delay);
            };
        }

        async function fetchAccounts() {
            const search = searchBox.value;
            const role = roleFilter.value;

            const url = '@Url.Action("Search", "Accounts")'
                + `?search=${encodeURIComponent(search)}&role=${encodeURIComponent(role)}`;

            const response = await fetch(url);
            const data = await response.json();

            tableBody.innerHTML = '';

            if (!data || data.length === 0) {
                tableBody.innerHTML =
                    `<tr><td colspan="6" class="text-center text-muted">No accounts found.</td></tr>`;
                return;
            }

            data.forEach(account => {
                tableBody.innerHTML += `
                    <tr>
                        <td>${account.fName}</td>
                        <td>${account.mName ?? ''}</td>
                        <td>${account.lName}</td>
                        <td>${account.email}</td>
                        <td>
                            <span class="badge ${account.role === 'Teacher' ? 'bg-primary' : 'bg-secondary'}">
                                ${account.role}
                            </span>
                        </td>
                        <td>
                            <a href="/Accounts/Edit/${account.accountID}" class="btn btn-sm btn-warning">Edit</a>
                            <a href="/Accounts/Delete/${account.accountID}" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>`;
            });
        }

        const debouncedFetch = debounce(fetchAccounts, 300);
        searchBox.addEventListener('input', debouncedFetch);
        roleFilter.addEventListener('change', fetchAccounts);
    </script>
}
